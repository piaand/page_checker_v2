# Change the cron expression to schedule the page check task
cron.expression=*/10 * * ? * ?

# Change the source of logging configurations
logging.path=./src/test/resources/loggingTest.properties

# Change the page checking configuration file name
pagecheck.configuration=./config_request.txt
